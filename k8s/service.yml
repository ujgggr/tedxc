apiVersion: v1
kind: Service
metadata:
  name: eivv
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "eivv"
    port: 8080
  selector:
    app: eivv
