apiVersion: v1
kind: Service
metadata:
  name: doqn
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "doqn"
    port: 8080
  selector:
    app: doqn
