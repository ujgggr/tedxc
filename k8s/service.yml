apiVersion: v1
kind: Service
metadata:
  name: ofoe
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ofoe"
    port: 8080
  selector:
    app: ofoe
