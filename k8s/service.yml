apiVersion: v1
kind: Service
metadata:
  name: dvip
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "dvip"
    port: 8080
  selector:
    app: dvip
