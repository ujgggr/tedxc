apiVersion: v1
kind: Service
metadata:
  name: bsos
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "bsos"
    port: 8080
  selector:
    app: bsos
