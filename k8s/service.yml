apiVersion: v1
kind: Service
metadata:
  name: rtvo
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "rtvo"
    port: 8080
  selector:
    app: rtvo
