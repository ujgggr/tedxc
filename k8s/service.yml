apiVersion: v1
kind: Service
metadata:
  name: geag
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "geag"
    port: 8080
  selector:
    app: geag
