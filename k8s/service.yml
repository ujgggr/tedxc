apiVersion: v1
kind: Service
metadata:
  name: aidb
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "aidb"
    port: 8080
  selector:
    app: aidb
