apiVersion: v1
kind: Service
metadata:
  name: ycni
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ycni"
    port: 8080
  selector:
    app: ycni
