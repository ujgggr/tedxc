apiVersion: v1
kind: Service
metadata:
  name: jabng
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "jabng"
    port: 8080
  selector:
    app: jabng
