apiVersion: v1
kind: Service
metadata:
  name: uldi
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "uldi"
    port: 8080
  selector:
    app: uldi
