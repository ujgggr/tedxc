apiVersion: v1
kind: Service
metadata:
  name: jiaup
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "jiaup"
    port: 8080
  selector:
    app: jiaup
