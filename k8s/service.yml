apiVersion: v1
kind: Service
metadata:
  name: yiss
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "yiss"
    port: 8080
  selector:
    app: yiss
