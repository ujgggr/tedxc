apiVersion: v1
kind: Service
metadata:
  name: emao
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "emao"
    port: 8080
  selector:
    app: emao
