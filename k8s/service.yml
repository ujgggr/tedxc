apiVersion: v1
kind: Service
metadata:
  name: pgtou
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "pgtou"
    port: 8080
  selector:
    app: pgtou
