apiVersion: v1
kind: Service
metadata:
  name: caomo
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "caomo"
    port: 8080
  selector:
    app: caomo
